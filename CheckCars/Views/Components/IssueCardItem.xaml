<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="CheckCars.Views.Components.IssueCardItem"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Padding="10">

    <Frame
        BorderColor="LightGray"
        CornerRadius="8"
        HasShadow="True"
        BackgroundColor="White"
        Padding="10">

        <VerticalStackLayout Spacing="8">
            <Label
                Text="Issue Details"
                FontAttributes="Bold"
                FontSize="18"
                TextColor="#333" />

            <Label
                Text="{Binding Details}"
                FontSize="14"
                TextColor="#666" />

            <Label
                Text="Priority:"
                FontAttributes="Bold"
                FontSize="14"
                TextColor="#333" />
            <Label
                Text="{Binding Priority}"
                FontSize="14"
                TextColor="#666" />

            <Label
                Text="Created:"
                FontAttributes="Bold"
                FontSize="14"
                TextColor="#333" />
            <Label
                Text="{Binding Created}"
                FontSize="14"
                TextColor="#666" />
        </VerticalStackLayout>
    </Frame>
</ContentView>
